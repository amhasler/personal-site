import React, { useState, useEffect } from 'react'
import '../app/App.scss';

function Case3({ showTopNavMenu }) {
  //const showNav = {
  //  display: showTopNavMenu ? 'flex' : 'none'
  //}
  //const showContact = {
  //  display: showTopNavMenu ? 'flex' : 'none'
  //}
  //const showMenuIcon = {
  //  display: showTopNavMenu ? 'none' : 'flex',
  //}
  return (
     <p>case3</p>
  )
}

export default Case3;
